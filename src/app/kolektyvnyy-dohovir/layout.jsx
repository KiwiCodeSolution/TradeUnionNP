export const metadata = {
  title: "Колективний договір",
  description: "Колективний договір",
};

export default function CollectiveAgreementLayout({ children }) {
  return <section>{children}</section>;
}
